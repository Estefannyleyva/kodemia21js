let frutas = []; //inicializar un arreglo vacio
let indice = 0;
function comprarFrutas(nombreFruta) {
    frutas[indice] = nombreFruta;
    indice++;
}

comprarFrutas('manzana');
comprarFrutas('fres');
comprarFrutas('uva');
comprarFrutas('cereza');
console.log(futas);

const arreglo =[1,2,3];
arreglo[0] = 'string';

const verduras = new Array();

comsole.log(verduras);