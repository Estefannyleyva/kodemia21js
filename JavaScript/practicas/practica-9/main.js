// Retornar un objeto con los nombres de los 
// usuarios ordenados por edad, de mayor a menor
const users = [
    {name: 'aldo', age: 18}, 
    {name: 'magaly', age: 30},
    {name: 'alfonso', age: 20}, 
    {name: 'jesus', age: 50}, 
    {name: 'alberto', age: 10}
];
// const result = {
//     0: ‘aldo’,
//     1: ‘magaly’,
//     2: ‘alfonso’,
//     3: ‘jesus’,
//     4: ‘alberto’
// };
function callback (users) {
    let actual = 0;
    let contador = 1;
    let resultado = [];
    for (let index = 0; index < serie-1; index++) {
    if (this[actual].age > this[contador].age) {
        resultado[actual]= 
    }
       resultado = actual + contador;
       actual = contador;
       contador = resultado;
       console.log(resultado);
    }
    console.log(resultado);

    let newUsers = {};
    let newUsers2 = 0;
    for(let i=0; i<user.length;  i++){
        newUsers= (user[i].name);
        console.log(newUsers)
    }
    
    let name = [];

    for (let i=0; i<newUsers.length;  i++){
        
    }
    
    return name;
}

Array.prototype.sort = (callback) =>{
    return this;
}
console.log(users.sort(callback))